# 커밋 & 푸시

변경된 파일을 커밋하고 푸시합니다.

## 수행 작업

1. `git status`로 변경 사항 확인
2. 변경 내용을 요약해서 커밋 메시지 작성 (한국어)
3. `git add -A` → `git commit -m "메시지"` → `git push` 순서로 실행
4. 결과 확인

## 규칙

- 커밋 메시지는 한국어로 작성
- 변경 내용을 명확하게 요약 (예: "검색 기능 개선: 기간 필터 추가")
- 빈 커밋은 만들지 않음
